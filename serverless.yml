service: voa-mobile-server

plugins:
  - serverless-plugin-typescript

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: us-east-1
  profile: excella

functions:
  articles:
    handler: handler.myAction
    events:
      - http:
          path: myAction
          method: get